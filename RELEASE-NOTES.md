Research snapshot: recovered Engine DJ stems pipeline, experimental offline renderer, original-worker numerical validation, model/precision/Pi research, disassembly evidence and listening comparisons. Read CURRENT-FINDINGS.md first; older notes are retained as history.

The complete ZIP contains 361 files (157 MB), including reference and A/B audio. The repository exposes the notes, code and measurements for browsing. No private model weights, firmware/worker binaries, credentials or session captures are included. Four stereo stem outputs; semantic identities remain unverified. This is a research handoff, not a BiteDJ implementation.

The .sha256 file verifies the ZIP. MANIFEST.json inside it contains per-file SHA-256 hashes; ZIP CRC and every payload hash were checked before upload.
